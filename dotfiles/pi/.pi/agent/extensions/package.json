{
  "name": "pi-extensions-dev",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "description": "Local dev types for pi extensions. pi injects the runtime packages; these devDependencies exist only for LSP type-checking.",
  "scripts": {
    "test": "node --test \"*.test.ts\" \"pi-tmux-subagents/*.test.ts\" \"pi-tmux-subagents/workflow/*.test.ts\"",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@earendil-works/pi-ai": "^0.84.4",
    "@earendil-works/pi-coding-agent": "^0.84.4",
    "@earendil-works/pi-tui": "^0.84.4",
    "@types/node": "^22.0.0",
    "typebox": "^1.3.10"
  }
}
