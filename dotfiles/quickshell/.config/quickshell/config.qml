import QtQuick

ShellRoot {
    // Main configuration file
    // This can be used for global settings and additional components

    // Import the main shell
    Loader {
        source: "shell.qml"
    }
}
